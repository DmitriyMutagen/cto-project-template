# Known Gotchas & Traps (Permanent Memory)

## Format
Framework/Tool | Gotcha | Workaround

## Gotchas
<!-- Add gotchas as discovered -->
<!-- Example:
- SQLAlchemy 2.0 | async session requires `expire_on_commit=False` | Set in session factory
- pytest-asyncio | Must use `@pytest.mark.asyncio` on every async test | Or set `asyncio_mode = "auto"` in pytest.ini
-->
